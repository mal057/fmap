{
  "name": "@fmap/shared-types",
  "version": "1.0.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "type-check": "tsc --noEmit",
    "lint": "echo 'No linting configured for shared-types'",
    "build": "tsc",
    "test": "echo 'No tests configured for shared-types'"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
